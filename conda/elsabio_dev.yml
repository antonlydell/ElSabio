name: elsabio_dev
channels:
  - defaults
  - conda-forge
dependencies:
  # Run
  - click >=8.0
  - pydantic >=2.0
  - pydantic-settings >=2.10
  - python >=3.12
  - python-duckdb >=1.0
  - streamlit >=1.46
  - streamlit_passwordless >=0.16
  - sqlalchemy >=2.0

  # Test
  - pytest >=7.0

  # Build
  - python-build >=1.0
  - setuptools >=77.0
  - twine >=6.0

  # Tools
  - ruff >=0.12
  - mypy >=1.0

  # Types
  - pandas-stubs >=2.0
